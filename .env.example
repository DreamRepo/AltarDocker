# =============================================================================
# ALTAR DOCKER - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and customize the values as needed.
# All variables have sensible defaults - you only need to change what you want.
#
# Usage: cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# MongoDB Configuration
# -----------------------------------------------------------------------------
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=changeme123
MONGO_DB=sacred
MONGO_PORT=27017
MONGO_DATA_PATH=./data/mongo/

# -----------------------------------------------------------------------------
# Omniboard Configuration
# -----------------------------------------------------------------------------
# Web dashboard for visualizing Sacred experiments
OMNIBOARD_HOST_PORT=9004
OMNIBOARD_DB=sacred

# Uncomment to enable a second Omniboard instance on a different database:
# OMNIBOARD_HOST_PORT_2=9003
# OMNIBOARD2_DB=test

# -----------------------------------------------------------------------------
# AltarExtractor Configuration
# -----------------------------------------------------------------------------
# Dash app for browsing and filtering experiment data
EXTRACTOR_PORT=8050

# -----------------------------------------------------------------------------
# MinIO Configuration (only needed with --profile minio)
# -----------------------------------------------------------------------------
# S3-compatible object storage for large artifacts
# Start with: docker compose --profile minio up -d
MINIO_ROOT_USER=minio_admin
MINIO_ROOT_PASSWORD=changeme123
MINIO_S3_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_DATA_PATH=./data/minio/